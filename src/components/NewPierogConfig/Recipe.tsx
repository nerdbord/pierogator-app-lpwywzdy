interface RecipeProps {
    inputValues: {
        dough: string;
        filling: string;
        ingreds: string;
      };    
 }
 
 const Recipe = (props: RecipeProps) => {
    return <></>;
 };
 
 export default Recipe;
 