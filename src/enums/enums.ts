export enum LockIconType {
   locked = 'locked',
   unlocked = 'unlocked',
}

export enum ButtonType {
   Primary = 'primary',
   Secondary = 'secondary',
}

export enum HeaderType {
   machine = 'machine',
   store = 'store'
}
